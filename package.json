{
  "name": "cursor-cost-guard",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "install:guard": "node install.mjs",
    "uninstall:guard": "node uninstall.mjs"
  }
}
